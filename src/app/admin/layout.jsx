
export default function AdminLayout({ children }){    
    return (
        <html>
            <head>
                <title>Malex Admin</title>
            </head>
            <body>
                { children }
            </body>
        </html>
    );
}
